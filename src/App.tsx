import React from 'react';
import './App.css';
import { split } from "./01"

const text = "Hello my friends"
const result = split(text);
console.log(result);



function App() {
  return (
    <div>

    </div>
  );
}

export default App;
